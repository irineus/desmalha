name: desmalha_app
description: "Desmalha — controle fiscal (carnê-leão) para autônomos que recebem via Pix."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # Núcleo fiscal (Dart puro) do monorepo — toda lógica de cálculo vive lá.
  desmalha_core:
    path: ../../packages/desmalha_core

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  sentry_flutter: ^9.27.0
  supabase_flutter: ^2.17.2

  # Persistência local cifrada (decisão travada: SQLite/Drift + SQLCipher).
  # O SQLCipher vem do próprio package:sqlite3 3.x via build hook — ver a
  # seção `hooks:` no fim deste arquivo. O antigo sqlcipher_flutter_libs
  # foi descontinuado (0.7.0+eol) e não entra aqui.
  drift: ^2.34.3
  sqlite3: ^3.5.1
  # Linha 9.x de propósito: a 11.x exige compileSdk 37, que o runner do
  # Codemagic ainda não resolve (ver comentário em android/app/build.gradle.kts).
  flutter_secure_storage: ^9.2.4
  path_provider: ^2.1.6
  path: ^1.9.1
  uuid: ^4.6.0
  connectivity_plus: ^7.3.1
  flutter_local_notifications: ^22.3.1
  timezone: ^0.11.1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  drift_dev: ^2.34.5
  build_runner: ^2.15.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    # Seed do catálogo versionado: o app calcula OFFLINE desde o primeiro
    # boot, antes de qualquer rede. Gerado do repositório por
    # `fvm dart run desmalha_core:gerar_seed_catalogo` e conferido por teste
    # (test/catalogo/seed_catalogo_test.dart) — não editar à mão.
    - assets/catalogo/seed.json

  # As três vozes do sistema visual (docs/design/desmalha-design-system.html),
  # EMBARCADAS: nenhuma requisição de rede para fonte, nenhum texto invisível
  # esperando download. Todas SIL OFL (licenças em assets/fonts/OFL-*.txt).
  # Fraunces e Karla são variáveis — um arquivo cada, o peso sai do eixo
  # `wght` (ver lib/tema/tipografia.dart); a Plex Mono não tem versão
  # variável e entra só nos três pesos usados.
  fonts:
    - family: Fraunces
      fonts:
        - asset: assets/fonts/Fraunces.ttf
    - family: Karla
      fonts:
        - asset: assets/fonts/Karla.ttf
    - family: IBMPlexMono
      fonts:
        - asset: assets/fonts/IBMPlexMono-Regular.ttf
          weight: 400
        - asset: assets/fonts/IBMPlexMono-Medium.ttf
          weight: 500
        - asset: assets/fonts/IBMPlexMono-SemiBold.ttf
          weight: 600

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Troca o SQLite embarcado pelo build SQLCipher publicado nos releases do
# package:sqlite3 (binários pré-compilados, verificados por sha256). Vale para
# TODAS as plataformas, inclusive o host dos testes — é o que permite provar
# `PRAGMA cipher_version` no `flutter test`, não só no aparelho.
# ⚠️ Remover esta seção faria o app voltar ao SQLite SEM CIFRA em silêncio;
# quem pega isso é o teste de boot em lib/dados/conexao_cifrada.dart.
hooks:
  user_defines:
    sqlite3:
      source: sqlcipher
