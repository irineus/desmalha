<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Lembrete LOCAL de vencimento do DARF (flutter_local_notifications):
         o plugin reagenda os avisos depois de reiniciar o aparelho. Sem
         SCHEDULE_EXACT_ALARM/USE_EXACT_ALARM de propósito: um lembrete de
         DATA não justifica alarme exato (e a Play audita essa permissão) —
         o agendamento é inexato (inexactAllowWhileIdle). POST_NOTIFICATIONS
         vem do manifesto do próprio plugin. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- allowBackup=false: o banco local cifrado NÃO pode ir para o backup
         automático do Google — deixar o SO copiar o arquivo para uma nuvem
         que não controlamos fura o claim "nem nós conseguimos ver seus
         dados". O backup do usuário é o envelope .dsmb cifrado ponta a
         ponta (card "Backup cifrado ponta a ponta"). Decisão registrada na
         modelagem local-first (ago/2026). -->
    <application
        android:label="desmalha_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Extrato vindo de outro app: "Compartilhar -> Desmalha" e
                 "Abrir com". Mesmos MIMEs do seletor
                 (lib/importacao/seletor_arquivo_sistema.dart): o Android nao
                 tem MIME para .ofx, que chega como octet-stream. Lido por
                 MainActivity.kt; a previa decide pelo conteudo. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/octet-stream"/>
                <data android:mimeType="application/x-ofx"/>
                <data android:mimeType="application/ofx"/>
                <data android:mimeType="application/vnd.intu.qfx"/>
                <data android:mimeType="text/x-ofx"/>
                <data android:mimeType="text/csv"/>
                <data android:mimeType="text/comma-separated-values"/>
                <data android:mimeType="application/csv"/>
                <data android:mimeType="application/vnd.ms-excel"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="content"/>
                <data android:mimeType="application/octet-stream"/>
                <data android:mimeType="application/x-ofx"/>
                <data android:mimeType="application/ofx"/>
                <data android:mimeType="application/vnd.intu.qfx"/>
                <data android:mimeType="text/x-ofx"/>
                <data android:mimeType="text/csv"/>
                <data android:mimeType="text/comma-separated-values"/>
                <data android:mimeType="application/csv"/>
                <data android:mimeType="application/vnd.ms-excel"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: abrir o texto dos Termos e da Política (https) no
             navegador, na tela de aceite do onboarding. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
