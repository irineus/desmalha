/// Versão do app gravada no manifesto de cada backup (`app_versao`).
///
/// Espelha o `version:` do pubspec.yaml — o teste `versao_test.dart`
/// reprova se os dois divergirem.
const String versaoDoApp = '1.0.0+1';
